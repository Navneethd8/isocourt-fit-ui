/**
 * Flat stage shells — colors from the SWECC navbar mark (lavender / lilac / plum), not site CSS.
 * No gradients: solid fills + single hairline borders only.
 */
export const panelClass =
  'relative overflow-hidden rounded-lg border border-[#3d3250] bg-[#16121d] p-5'

/** Tinted lilac stage — `!` beats `.demo-panel--light { background: var(--iso-shelf) }` in App.css. */
export const panelClassLight =
  'relative overflow-hidden rounded-lg !border !border-solid !border-[#cfc4df] !bg-[#ebe4f4] p-5'
