/** Logo sage (~#7ea266 from SWECC lockup) on grey-green fields — flat panels, no gradients. */
export const panelClass =
  'relative overflow-hidden rounded-lg border border-solid border-[#3d423c] bg-[#1a1d18] p-5'

/** Sage-washed grey stage — same idea as dark column; `!` overrides global light-panel shelf fill. */
export const panelClassLight =
  'relative overflow-hidden rounded-lg !border !border-solid !border-[#aeb8a8] !bg-[#e2e8dd] p-5'
