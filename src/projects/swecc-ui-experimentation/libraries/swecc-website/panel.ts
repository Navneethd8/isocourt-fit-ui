/** Stage fills from swecc.org production `main.*.css` — `--bg` light / dark. Flat, no gradients. */
export const panelClass =
  'relative overflow-hidden rounded-[8px] border border-solid border-[#474a4d] bg-[#2c2d2e] p-5'

/** Soft purple-grey paper (production `--bg` feel, visibly tinted vs plain shelf). */
export const panelClassLight =
  'relative overflow-hidden rounded-[8px] !border-[1.5px] !border-solid !border-[#474a4d] !bg-[#f2f0fb] p-5'
