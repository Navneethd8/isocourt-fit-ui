/**
 * Pane bar copy and “wide row” threshold: the grid always renders every visible kit; when there are more
 * than this many columns, the row scrolls horizontally instead of shrinking tracks.
 */
export const MAX_PANES_IN_GRID = 4
